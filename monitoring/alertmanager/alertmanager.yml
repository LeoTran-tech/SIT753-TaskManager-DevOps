global:
  resolve_timeout: 1m

route:
  receiver: local-monitoring
  group_by:
    - alertname
    - environment
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: local-monitoring
